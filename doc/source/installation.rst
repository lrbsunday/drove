Installation
============

You can install drove using the normal way for python packages:

.. code-block:: sh

  $ pip install drove

In the future we provide also binaries for different platfmors, which
probably will include a set of plugins also.
